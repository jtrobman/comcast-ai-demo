NEXT_PUBLIC_API_URL="http://127.0.0.1:8000"
ASSET_PREFIX=""
CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

PORT=3000 # Next.js (web) port — read natively by `next dev` / `next start`
UVICORN_PORT=8000 # uvicorn (api) port — read natively by uvicorn

OPENAI_API_KEY=""
OPENAI_MODEL="gpt-5-nano"
OPENAI_REASONING_EFFORT="minimal"
OPENAI_PROMPT_CACHE_KEY="comcast-resolution-copilot-20260508"

VOYAGE_API_KEY=""
VOYAGE_EMBED_MODEL="voyage-3.5-lite"
